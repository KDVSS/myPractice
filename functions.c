// C program to add two numbers
#include <stdio.h>

 int A =10, B=30,C = 15, sum, multiply;
 float D;


int main()
{
    sum = A + B;
    multiply = B*C;

  
    // Print the sum
    printf("Sum of A and B is: %d\n", sum);
    
    printf("Multiply of B and C is: %d", multiply);
  
    return 0;
}